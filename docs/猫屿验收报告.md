# 猫屿 CAT ISLE · M4 验收报告

| 项目 | 内容 |
| --- | --- |
| 依据 | PRD V1.2 §11 验收标准（11 项） |
| 验证方式 | 端到端联调脚本（scripts/e2e_smoke.py，23 项断言）+ 补充验收脚本（scripts/verify_prd.py，5 项断言）+ pytest（22 用例） |
| 日期 | 2026-08-27 |
| 结论 | **11/11 项验收通过**（其中响应式项为代码断点确认，浏览器最终视觉验收见备注） |

---

## 验收结果明细

| # | 验收项 | 结果 | 验证证据 |
| --- | --- | --- | --- |
| 1 | 预约全流程 | ✅ | e2e：提交预约(pending_payment) → 凭证(payment_verify) → 核验(confirmed) → 到店(completed) 全链路通过 |
| 2 | 押金超时 | ✅ | verify_prd：3 小时前创建的 pending_payment 记录经扫描自动变为 cancelled |
| 3 | 店休日 | ✅ | e2e：周一 slots 接口返回 `is_holiday=true`；非店休日返回 4 时段余量 |
| 4 | 领养流程 | ✅ | e2e：申请 → interview → home_check → adopted（含 success_story）→ 回访登记/查询 → 前台案例墙可见 |
| 5 | 后台鉴权 | ✅ | e2e：未登录访问 /api/admin/cats 返回 401；登录后 JWT 全程可用 |
| 6 | 内容管理 | ✅ | e2e：后台新增猫咪前台立即可见；下线后立即不可见；软删除后列表不显示 |
| 7 | 响应式 | ✅* | 代码层面：全部页面实现 375/768/1280 三档断点（grid-cols 降列、md: 前缀、移动端底部预约栏/汉堡导航），无障碍 reduced-motion 生效 |
| 8 | 数据安全 | ✅ | e2e：删除为软删除（deleted=1）；verify_prd：.txt 上传被拒（40001）；>5MB 超限被拒 |
| 9 | 防超卖 | ✅ | pytest：10 线程占满 6 名额后第 7 单返回 40002；同手机号同日重复提交 40003；`BEGIN IMMEDIATE` 写锁 + 部分唯一索引双保险 |
| 10 | 顾客自助取消 | ✅ | e2e：核验前自助取消成功；取消后同手机号同日可重新预约（名额与唯一索引均释放） |
| 11 | 图片压缩 | ✅ | verify_prd：3.4MB 噪点图上传 → 压缩至 1.0MB（降 71%），长边 2000→1600 |

\* 响应式项：断点与组件均已实现并通过构建验证，浏览器最终视觉走查建议在部署预览地址执行（原型与 UIUX §8 对照）。

## 测试资产

| 脚本 | 位置 | 断言数 |
| --- | --- | --- |
| 端到端联调 | `cat-isle-backend/scripts/e2e_smoke.py` | 23（含 11 项验收映射） |
| PRD 补充验收 | `cat-isle-backend/scripts/verify_prd.py` | 5 |
| 单元测试 | `cat-isle-backend/tests/` | 22 |

## 运行方式

```bash
# 1. 启动后端（单实例，必须 --workers 1）
ADMIN_USERNAME=admin ADMIN_PASSWORD=<密码> uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1

# 2. 执行验收（另开终端）
ADMIN_PASSWORD=<密码> python scripts/e2e_smoke.py   # 23 断言
python scripts/verify_prd.py                          # 5 断言（需后端运行）
```

## 验收结论

**PRD §11 十一项验收全部通过。** 系统达到可部署状态；上线前置素材（猫咪实拍、真实菜单、收款码、服务器域名）就绪后即可正式上线。
