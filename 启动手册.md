# 猫屿 CAT ISLE · 启动手册

> 版本：V1.1（2026-08-27）· 适用：开发预览 / 本地运行
> 正式上线请参照 `deploy/上线检查清单.md`（服务器、HTTPS、备份等）
> **V1.1 更新：全部命令改为绝对路径**（解决终端默认目录与项目路径不一致导致的"找不到路径"问题）

---

## 0. 路径说明（重要，先读）

### 0.1 本机项目绝对路径

| 用途 | 绝对路径 |
| --- | --- |
| 项目仓库根 | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-` |
| 后端代码（仓库版，含 `.env`、`static/` 前端产物，**服务当前运行在这**） | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend` |
| 后端代码（工作区版，含虚拟环境 `.venv`） | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend` |
| 前端源码 | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/frontend` |
| 虚拟环境 Python | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe` |

### 0.2 为什么之前会"找不到路径"

终端默认打开的位置（如用户主目录）**不包含** `cat-isle-backend` 目录，因此 `cd cat-isle-backend`（相对路径）会失败。
**解决**：本手册所有 `cd` 均使用上面表格的绝对路径；执行前不必先切到项目目录。

### 0.3 路径变更提示

若项目移动到其他位置，请**全局替换**路径前缀 `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18` 为新位置即可。

---

## 1. 方式 A：单端口启动（推荐，最简）

**前端页面与 API 都由 8000 端口提供**（前端构建产物已内置在 `static/`）。

```bash
# 1) 进入后端目录（绝对路径，任何终端位置都可执行）
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend"

# 2) 首次准备：创建虚拟环境 + 安装依赖（只需一次）
"C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe" -m venv "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend/.venv"
# 若 venv 已存在（如工作区版已装好），可直接复用：
# "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/pip.exe" install -r requirements.txt

# 3) 首次准备：配置环境变量（修改 JWT_SECRET 与 ADMIN_PASSWORD）
cp .env.example .env
# 或直接复制已配置好的：cp "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend/.env" .

# 4) 初始化数据库 + 占位数据（幂等，可重复执行）
"C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe" -m app.seed_demo

# 5) 启动服务（生产必须 --workers 1：内嵌定时任务，多 worker 会重复执行）
"C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe" -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
```

访问：
- 前台首页：http://127.0.0.1:8000
- 后台管理：http://127.0.0.1:8000/admin（默认账号：`.env` 中的 ADMIN_USERNAME / ADMIN_PASSWORD，演示值 admin / CatIsle2026）
- API 文档：http://127.0.0.1:8000/docs

---

## 2. 方式 B：开发模式（前端热更新）

```bash
# 终端 1：后端
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend"
"C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe" -m uvicorn app.main:app --port 8000 --reload

# 终端 2：前端（/api 自动代理到 8000，热更新）
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/frontend"
npm install          # 首次
npm run dev          # 访问 http://localhost:5173
```

---

## 3. 数据说明

| 命令（完整路径前缀 `PY="C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe"`） | 作用 |
| --- | --- |
| `$PY -m app.seed` | 基础初始化：建表 + 4 时段 + 门店默认配置 + 管理员 + 部分唯一索引（应用启动时自动执行） |
| `$PY -m app.seed_demo` | 占位数据：6 只猫咪 + 10 项菜单（开发演示用；上线前店主在后台替换真实素材） |
| 后台录入 | 真实数据：「猫咪管理」「餐单管理」「门店设置」（收款码/地址/押金/地图） |

---

## 4. 常用运维命令

### 查看 / 停止 / 重启（按端口）

```bash
# 查看 8000 端口占用（PowerShell）
powershell -Command "Get-NetTCPConnection -LocalPort 8000 -State Listen | Select-Object OwningProcess"

# 停止（PowerShell）
powershell -Command "Get-NetTCPConnection -LocalPort 8000 -State Listen | ForEach-Object { Stop-Process -Id \$_.OwningProcess -Force }"

# systemd 部署环境（Linux 服务器）
sudo systemctl restart catisle
journalctl -u catisle -f
```

### 更新前端产物（改完前端代码后）

```bash
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/frontend"
npm run build
cp -r dist/* "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend/static/"
# 重启后端即生效
```

### 数据库备份（手动）

```bash
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend"
sqlite3 catisle.db ".backup backup_$(date +%Y%m%d).db"
```

---

## 5. 常见问题

| 现象 | 处理 |
| --- | --- |
| 提示"找不到路径" | 使用本文档的**绝对路径**（见 §0），不要用相对路径 `cd cat-isle-backend` |
| 端口被占用启动失败 | 按 §4「停止」命令清理后重启 |
| 忘记管理员密码 | 修改 `.env` 中 ADMIN_PASSWORD 后重启；已存在的管理员不会被 seed 覆盖，需手动改库（`UPDATE admin_user SET password_hash=...`） |
| 前端页面是旧版本 | 重新执行 §4「更新前端产物」 |
| 需要重置数据库 | 停止服务 → 删除 `catisle.db` → `python -m app.seed_demo` → 启动 |

---

## 6. 正式上线

本地验证通过后，按 `deploy/上线检查清单.md` 执行：服务器环境 → 代码部署 → HTTPS → 数据初始化 → 功能回归 → 备份保障（素材与服务器就绪后约 1 天内完成）。
