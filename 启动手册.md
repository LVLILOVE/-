# 猫屿 CAT ISLE · 启动手册

> 版本：V1.2（2026-08-27）
> 适用：开发预览 / 本地运行；正式上线请参照 `deploy/上线检查清单.md`
> **终端约定：本手册命令以 Git Bash 为准**（Windows 自带 CMD/PowerShell 请对应调整语法，如 `cp` → `copy`）

---

## 0. 路径说明（重要，先读）

### 0.1 本机项目绝对路径

| 用途 | 绝对路径 |
| --- | --- |
| 项目仓库根 | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-` |
| 后端代码（仓库版，含 `.env`、`static/` 前端产物，**推荐运行目录**） | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend` |
| 后端代码（工作区版，含虚拟环境 `.venv`，**Python 解释器来源**） | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend` |
| 前端源码 | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/frontend` |
| 虚拟环境 Python（已装好全部依赖） | `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe` |

> **运行约定**：在「仓库版」目录执行代码，使用「工作区版」的 Python 解释器（依赖已装好，无需激活 venv）。
> 为便于阅读，下文用 `$PY` 指代上面的虚拟环境 Python 完整路径（Git Bash 变量）。

### 0.2 为什么之前会"找不到路径"

终端默认打开的位置（如用户主目录）**不包含** `cat-isle-backend` 目录，因此 `cd cat-isle-backend`（相对路径）会失败。
**解决**：本手册所有 `cd` 均使用绝对路径；执行前不必先切到项目目录。

### 0.3 路径变更提示

若项目移动到其他位置，请**全局替换**路径前缀 `C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18` 为新位置即可。

---

## 1. 方式 A：单端口启动（推荐，最简）

**前端页面与 API 都由 8000 端口提供**（前端构建产物已内置在 `static/`）。

```bash
# 0) 定义解释器变量（每次新开终端执行一次）
PY="C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe"

# 1) 进入后端目录（仓库版）
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend"

# 2) 首次准备：确认 .env 存在（不存在则创建；请修改 JWT_SECRET 与 ADMIN_PASSWORD）
#    已有 .env（服务当前使用的这份已配好）则跳过
ls .env || cp .env.example .env

# 3) 初始化数据库 + 占位数据（幂等，可重复执行；不想要占位数据可用 $PY -m app.seed）
"$PY" -m app.seed_demo

# 4) 启动服务（生产必须 --workers 1：内嵌定时任务，多 worker 会重复执行）
"$PY" -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
```

> 首次使用且「工作区版」venv 不存在时的替代（只需一次）：
> 用系统 Python 创建仓库版 venv 并安装依赖：
> ```bash
> python -m venv "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend/.venv"
> "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend/.venv/Scripts/pip.exe" install -r requirements.txt
> # 然后把上面 $PY 指向仓库版 venv：.../-/cat-isle-backend/.venv/Scripts/python.exe
> ```

访问：
- 前台首页：http://127.0.0.1:8000
- 后台管理：http://127.0.0.1:8000/admin（默认账号：`.env` 中的 ADMIN_USERNAME / ADMIN_PASSWORD，演示值 admin / CatIsle2026）
- API 文档：http://127.0.0.1:8000/docs

---

## 2. 方式 B：开发模式（前端热更新）

```bash
PY="C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/cat-isle-backend/.venv/Scripts/python.exe"

# 终端 1：后端（--reload 代码变更自动重启）
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend"
"$PY" -m uvicorn app.main:app --port 8000 --reload

# 终端 2：前端（/api 自动代理到 8000，热更新）
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/frontend"
npm install          # 首次
npm run dev          # 访问 http://localhost:5173
```

---

## 3. 数据说明

| 命令（`$PY` 定义见 §1） | 作用 |
| --- | --- |
| `"$PY" -m app.seed` | 基础初始化：建表 + 4 时段 + 门店默认配置 + 管理员 + 部分唯一索引（应用启动时自动执行） |
| `"$PY" -m app.seed_demo` | 占位数据：6 只猫咪 + 10 项菜单（开发演示用；上线前店主在后台替换真实素材） |
| 后台录入 | 真实数据：「猫咪管理」「餐单管理」「门店设置」（收款码/地址/押金/地图） |

---

## 4. 常用运维命令

### 查看 / 停止 / 重启（按端口）

```bash
# 查看 8000 端口占用（PowerShell）
powershell -Command "Get-NetTCPConnection -LocalPort 8000 -State Listen | Select-Object OwningProcess"

# 停止（PowerShell）
powershell -Command "Get-NetTCPConnection -LocalPort 8000 -State Listen | ForEach-Object { Stop-Process -Id \$_.OwningProcess -Force }"

# systemd 部署环境（Linux 服务器）
sudo systemctl restart catisle
journalctl -u catisle -f
```

### 更新前端产物（改完前端代码后）

```bash
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/frontend"
npm run build
cp -r dist/* "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend/static/"
# 重启后端即生效
```

### 数据库备份（手动）

```bash
cd "C:/Users/Administrator/WorkBuddy/2026-08-26-14-15-18/-/cat-isle-backend"
sqlite3 catisle.db ".backup backup_$(date +%Y%m%d).db"
```

---

## 5. 常见问题

| 现象 | 处理 |
| --- | --- |
| 提示"找不到路径" | 使用本文档**绝对路径**（见 §0），不要用相对路径 `cd catisle-backend` |
| 端口被占用启动失败 | 按 §4「停止」命令清理后重启 |
| 忘记管理员密码 | 改 `.env` 的 ADMIN_PASSWORD 后重启仅对新初始化生效；已存在的管理员需手动改库：`sqlite3 catisle.db "UPDATE admin_user SET password_hash='<bcrypt哈希>';"`（哈希可用 `$PY -c "from app.core.security import hash_password; print(hash_password('新密码'))"` 生成） |
| 前端页面是旧版本 | 重新执行 §4「更新前端产物」 |
| 需要重置数据库 | 停止服务 → 删除 `catisle.db` → `"$PY" -m app.seed_demo` → 启动 |

---

## 6. 正式上线

本地验证通过后，按 `deploy/上线检查清单.md` 执行：服务器环境 → 代码部署 → HTTPS → 数据初始化 → 功能回归 → 备份保障（素材与服务器就绪后约 1 天内完成）。
