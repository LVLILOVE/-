# 猫屿 CAT ISLE · 启动手册

> 版本：V1.0（2026-08-27）· 适用：开发预览 / 本地运行
> 正式上线请参照 `deploy/上线检查清单.md`（服务器、HTTPS、备份等）

---

## 0. 目录结构速览

```
-/
├── cat-isle-backend/   # FastAPI 后端（含 static/ 前端产物，单端口运行）
├── frontend/           # React 前端源码（开发热更新用）
├── deploy/             # 部署配置（nginx / systemd / backup / 上线清单）
└── docs/               # 项目文档
```

---

## 1. 方式 A：单端口启动（推荐，最简）

**前端页面与 API 都由 8000 端口提供**（前端构建产物已内置在 `cat-isle-backend/static/`）。

```bash
cd cat-isle-backend

# 1) 首次准备：创建虚拟环境 + 安装依赖（Windows Git Bash 示例）
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt

# 2) 首次准备：配置环境变量（修改 JWT_SECRET 与 ADMIN_PASSWORD）
cp .env.example .env

# 3) 初始化数据库 + 占位数据（幂等，可重复执行）
python -m app.seed_demo

# 4) 启动服务（生产必须 --workers 1：内嵌定时任务，多 worker 会重复执行）
uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
```

访问：
- 前台首页：http://127.0.0.1:8000
- 后台管理：http://127.0.0.1:8000/admin（默认账号：.env 中的 ADMIN_USERNAME / ADMIN_PASSWORD）
- API 文档：http://127.0.0.1:8000/docs

---

## 2. 方式 B：开发模式（前端热更新）

```bash
# 终端 1：后端
cd cat-isle-backend
uvicorn app.main:app --port 8000 --reload

# 终端 2：前端（/api 自动代理到 8000）
cd frontend
npm install          # 首次
npm run dev          # http://localhost:5173
```

---

## 3. 数据说明

| 命令 | 作用 | 说明 |
| --- | --- | --- |
| `python -m app.seed` | 基础初始化 | 建表 + 4 时段 + 门店默认配置 + 管理员 + 部分唯一索引（应用启动时自动执行） |
| `python -m app.seed_demo` | 占位数据 | 6 只猫咪 + 10 项菜单（开发演示用；上线前由店主在后台替换真实素材） |
| 后台录入 | 真实数据 | 「猫咪管理」「餐单管理」「门店设置」（收款码/地址/押金/地图） |

---

## 4. 常用运维命令

### 查看/停止/重启

```bash
# 查看 8000 端口占用与进程
powershell "Get-NetTCPConnection -LocalPort 8000 -State Listen | Select-Object OwningProcess"

# 停止（Windows）
powershell "Get-NetTCPConnection -LocalPort 8000 -State Listen | ForEach-Object { Stop-Process -Id \$_.OwningProcess -Force }"

# systemd 部署环境
sudo systemctl restart catisle
journalctl -u catisle -f
```

### 更新前端产物（改完前端代码后）

```bash
cd frontend
npm run build
cp -r dist/* ../cat-isle-backend/static/
# 重启后端即生效
```

### 数据库备份（手动）

```bash
sqlite3 catisle.db ".backup backup_$(date +%Y%m%d).db"
```

---

## 5. 常见问题

| 现象 | 处理 |
| --- | --- |
| 端口被占用启动失败 | 按上方「停止」命令清理后重启 |
| 忘记管理员密码 | 删除 `.env` 中的 ADMIN_PASSWORD 重新设置，重启后执行 `python -m app.seed_demo`（不会覆盖已存在管理员，需手动改库） |
| 前端页面是旧版本 | 重新执行「更新前端产物」步骤 |
| 需要重置数据库 | 停止服务 → 删除 `catisle.db` → 重新 `python -m app.seed_demo` → 启动 |

---

## 6. 正式上线

本地验证通过后，按 `deploy/上线检查清单.md` 执行：服务器环境 → 代码部署 → HTTPS → 数据初始化 → 功能回归 → 备份保障（预计素材与服务器就绪后 1 天内完成）。
